<UserControl
    x:Class="ReportEngine.App.Views.Controls.ProjectPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:ReportEngine.App.Display"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReportEngine.App.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="500"
    d:DesignWidth="800"
    FontSize="13"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:ObvyazkaImagePathConverter x:Key="ObvyazkaImagePathConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Верхний левый GroupBox  -->
        <GroupBox
            Grid.Row="0"
            Grid.Column="0"
            Margin="5"
            Padding="5"
            Header="Стенды проекта">
            <ListView ItemsSource="{Binding CurrentProjectModel.Stands}" SelectedItem="{Binding CurrentProjectModel.SelectedStand, Mode=TwoWay}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn
                            Width="40"
                            DisplayMemberBinding="{Binding Number}"
                            Header="№" />
                        <GridViewColumn
                            Width="120"
                            DisplayMemberBinding="{Binding KKSCode}"
                            Header="KKS" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding Design}"
                            Header="Обозначение" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding BraceType}"
                            Header="Тип крепления" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding ObvyazkaType}"
                            Header="Тип обвязки" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding Sensor}"
                            Header="Тип датчика" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!--  Верхний правый GroupBox  -->
        <GroupBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="5"
            Padding="5"
            Header="Рамы в стенде">
            <ListView
                DisplayMemberPath="Name"
                ItemsSource="{Binding CurrentProjectModel.SelectedStand.FramesInStand}"
                SelectedItem="{Binding CurrentStandModel.SelectedFrame}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Наименование" />
                        <GridViewColumn DisplayMemberBinding="{Binding FrameType}" Header="Тип" />
                        <GridViewColumn DisplayMemberBinding="{Binding Width}" Header="Ширина" />
                        <GridViewColumn DisplayMemberBinding="{Binding Height}" Header="Высота" />
                        <GridViewColumn DisplayMemberBinding="{Binding Depth}" Header="Глубина" />
                        <GridViewColumn DisplayMemberBinding="{Binding Weight}" Header="Масса" />
                        <GridViewColumn DisplayMemberBinding="{Binding Designe}" Header="Описание по КД" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!--  Средний левый GroupBox  -->
        <GroupBox
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            Padding="5"
            Header="Обвязки в стенде">
            <ListView ItemsSource="{Binding CurrentProjectModel.SelectedStand.ObvyazkiInStand}" SelectedItem="{Binding CurrentProjectModel.SelectedStand.SelectedObvyazka, Mode=TwoWay}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding ObvyazkaId}"
                            Header="Тип" />
                        <GridViewColumn
                            Width="120"
                            DisplayMemberBinding="{Binding MaterialLine}"
                            Header="Материал" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding Armature}"
                            Header="Арматура" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding TreeSocket}"
                            Header="Тройник" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding KMCH}"
                            Header="КМЧ" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding FirstSensorType}"
                            Header="Датчик 1" />
                        <GridViewColumn Header="Картинка" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image
                                        Width="80"
                                        Height="80"
                                        Source="{Binding ImageName, Converter={StaticResource ObvyazkaImagePathConverter}}"
                                        Stretch="Uniform" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!--  Средний правый GroupBox с картинкой и информацией об обвязке  -->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Картинка выбранной обвязки  -->
            <Border
                Grid.Row="0"
                Margin="5"
                Background="WhiteSmoke"
                BorderBrush="LightGray"
                BorderThickness="1"
                CornerRadius="5">
                <Image
                    Width="150"
                    Height="150"
                    Source="{Binding CurrentProjectModel.ImageName, Converter={StaticResource ObvyazkaImagePathConverter}}"
                    Stretch="Uniform" />
            </Border>

            <!--  Данные обвязки  -->
            <GroupBox
                Grid.Row="1"
                Margin="5"
                Padding="5"
                Header="Информация об обвязке">
                <StackPanel>
                    <StackPanel.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="12" />
                            <Setter Property="Margin" Value="0,2" />
                        </Style>
                    </StackPanel.Resources>

                    <TextBlock Text="{Binding CurrentProjectModel.SelectedStand.SelectedObvyazka.ObvyazkaId, StringFormat='Тип: {0}'}" />
                    <TextBlock Text="{Binding CurrentProjectModel.SelectedStand.SelectedObvyazka.MaterialLine, StringFormat='Материал: {0}'}" />
                    <TextBlock Text="{Binding CurrentProjectModel.SelectedStand.SelectedObvyazka.Armature, StringFormat='Арматура: {0}'}" />
                    <TextBlock Text="{Binding CurrentProjectModel.SelectedStand.SelectedObvyazka.TreeSocket, StringFormat='Тройник: {0}'}" />
                    <TextBlock Text="{Binding CurrentProjectModel.SelectedStand.SelectedObvyazka.KMCH, StringFormat='КМЧ: {0}'}" />
                    <TextBlock Text="{Binding CurrentProjectModel.SelectedStand.SelectedObvyazka.FirstSensorType, StringFormat='Датчик 1: {0}'}" />
                </StackPanel>
            </GroupBox>
        </Grid>

        <!--  Нижний левый GroupBox  -->
        <GroupBox
            Grid.Row="2"
            Grid.Column="0"
            Margin="5"
            Padding="5"
            Header="Электрические компоненты">
            <ListView ItemsSource="{Binding CurrentProjectModel.SelectedStand.AllElectricalPurposesInStand}" SelectedItem="{Binding CurrentStandModel.SelectedElectricalComponent}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Purpose}" Header="Назначение" />
                        <GridViewColumn DisplayMemberBinding="{Binding Material}" Header="Материал" />
                        <GridViewColumn DisplayMemberBinding="{Binding Quantity}" Header="Кол-во" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!--  Нижний правый GroupBox  -->
        <GroupBox
            Grid.Row="2"
            Grid.Column="1"
            Margin="5"
            Padding="5"
            Header="Дренаж в стенде">
            <ListView ItemsSource="{Binding CurrentProjectModel.SelectedStand.AllDrainagePurposesInStand}" SelectedItem="{Binding CurrentStandModel.SelectedDrainage}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Purpose}" Header="Назначение" />
                        <GridViewColumn DisplayMemberBinding="{Binding Material}" Header="Материал" />
                        <GridViewColumn DisplayMemberBinding="{Binding Quantity}" Header="Кол-во" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
    </Grid>
</UserControl>
