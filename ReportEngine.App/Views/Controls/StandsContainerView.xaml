<UserControl
    x:Class="ReportEngine.App.Views.Controls.StandsContainerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{DynamicResource PrimaryColor}"
    FontSize="16"
    Foreground="{DynamicResource PrimaryForeground}"
    mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <GroupBox
            Grid.Row="0"
            Grid.Column="0"
            Margin="5"
            Padding="5"
            Header="Содание партии">
            <DockPanel LastChildFill="True">
                <DataGrid
                    AutoGenerateColumns="False"
                    BeginningEdit="DataGrid_BeginningEdit"
                    CanUserAddRows="True"
                    CanUserDeleteRows="True"
                    EnableColumnVirtualization="False"
                    ItemsSource="{Binding CurrentProjectModel.ContainerBatchesInProject}"
                    LoadingRow="DataGrid_LoadingRow"
                    MouseDoubleClick="DataGrid_MouseDoubleClick"
                    SelectedItem="{Binding CurrentProjectModel.SelectedContainerBatch}">
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Command="{Binding ProjectCommandProvider.CreateContainerBatchCommand}" Header="Добавить партию" />
                            <Separator />
                            <MenuItem Command="{Binding ProjectCommandProvider.RefreshBatchesCommand}" Header="Обновить список" />
                            <Separator />
                            <MenuItem Command="{Binding ProjectCommandProvider.DeleteBatchCommand}" Header="Удалить упаковку проекта" />
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="Auto"
                            Binding="{Binding BatchOrder}"
                            Header="№" />
                        <DataGridTextColumn
                            Width="Auto"
                            Binding="{Binding ContainersCount}"
                            Header="Количество тары" />
                        <DataGridTextColumn
                            Width="Auto"
                            Binding="{Binding StandsCount}"
                            Header="Количество стендов" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <GroupBox
            Grid.Row="1"
            Grid.Column="0"
            MinHeight="300"
            Margin="5"
            Padding="5">
            <GroupBox.Header>
                <TextBlock>
                    <Run Text="Упаковка №" />
                    <Run
                        FontSize="14"
                        FontWeight="Bold"
                        Text="{Binding CurrentProjectModel.SelectedContainerBatch.BatchOrder, TargetNullValue='(ошибка привязки)'}" />
                </TextBlock>
            </GroupBox.Header>
            <DockPanel LastChildFill="True">
                <DataGrid
                    BeginningEdit="DataGrid_BeginningEdit"
                    CanUserAddRows="True"
                    CanUserDeleteRows="True"
                    IsReadOnly="False"
                    ItemsSource="{Binding CurrentProjectModel.ContainerStandsInSelectedBatch}"
                    LoadingRow="DataGrid_LoadingRow"
                    MouseDoubleClick="DataGrid_MouseDoubleClick"
                    SelectedItem="{Binding CurrentProjectModel.SelectedContainerStand}">
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Command="{Binding ProjectCommandProvider.AddContainerToBatchCommand}" Header="Добавить упаковку в партию" />
                            <Separator />
                            <MenuItem Command="{Binding ProjectCommandProvider.UpdateContainerCommand}" Header="Сохранить изменения" />
                            <Separator />
                            <MenuItem Command="{Binding ProjectCommandProvider.DeleteContainerCommand}" Header="Удалить упаковку" />
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Name}" Header="Тара" />
                        <DataGridTextColumn
                            Width="Auto"
                            Binding="{Binding StandsCount}"
                            Header="Количество стендов" />
                        <DataGridTextColumn
                            Width="Auto"
                            Binding="{Binding StandsWeight}"
                            Header="Масса стендов" />
                        <DataGridTextColumn
                            Width="Auto"
                            Binding="{Binding ContainerWeight}"
                            Header="Масса" />
                        <DataGridTextColumn
                            Width="Auto"
                            Binding="{Binding ContainerCost}"
                            Header="Стоимость" />
                        <DataGridTemplateColumn Header="Выбрать">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button
                                        Margin="2,0,2,0"
                                        Padding="5,0,5,0"
                                        Command="{Binding DataContext.ProjectCommandProvider.OpenAllSortamentsDialogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Content="Выбрать" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <Button
                Width="120"
                Margin="5"
                Command="{Binding ProjectCommandProvider.AddStandToContainerCommand}"
                Content="&lt;&lt;&lt; Добавить" />

            <Button
                Width="120"
                Margin="5"
                Command="{Binding ProjectCommandProvider.RemoveStandFromContainerCommand}"
                Content="Удалить &gt;&gt;&gt;" />
        </StackPanel>

        <GroupBox
            Grid.Row="1"
            Grid.Column="2"
            Margin="4"
            Header="Стенды проекта">
            <DataGrid
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                IsReadOnly="True"
                ItemsSource="{Binding CurrentProjectModel.Stands}"
                SelectedItem="{Binding CurrentProjectModel.SelectedStandInProject}">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Command="{Binding ProjectCommandProvider.AddStandToContainerCommand}" Header="Добавить выбранный стенд в контейнер" />
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding KKSCode}" Header="KKS" />
                    <DataGridTextColumn
                        Width="140"
                        Binding="{Binding SerialNumber}"
                        Header="Серийный номер" />
                    <DataGridTextColumn
                        Width="80"
                        Binding="{Binding NN}"
                        Header="Ширина" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox
            Grid.Row="3"
            Grid.Column="0"
            Margin="5"
            Padding="5"
            Header="Стенды">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <GroupBox
                    Grid.Row="0"
                    Margin="4"
                    Header="Стенды внутри тары">
                    <DataGrid
                        Margin="4,24,4,4"
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        ItemsSource="{Binding CurrentProjectModel.StandsInSelectedContainer}"
                        SelectedItem="{Binding CurrentProjectModel.SelectedStandInContainer}">
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Command="{Binding ProjectCommandProvider.RemoveStandFromContainerCommand}" Header="Удалить стенд из упаковки" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding KKSCode}" Header="KKS" />
                            <DataGridTextColumn
                                Width="140"
                                Binding="{Binding SerialNumber}"
                                Header="Серийный номер" />
                            <DataGridTextColumn
                                Width="100"
                                Binding="{Binding Width}"
                                Header="Ширина" />
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>