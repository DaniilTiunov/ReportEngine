<Window
    x:Class="ReportEngine.App.Views.Settings.CalculationParameters.CalculationParametersWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Настройки параметров расчета"
    Width="1300"
    Height="900"
    Background="{DynamicResource PrimaryColor}"
    FontFamily="Bahnschrift"
    FontSize="14"
    Foreground="{DynamicResource PrimaryForeground}"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            CornerRadius="0"
            GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>

    <Border Style="{DynamicResource WindowBorder}">
        <DockPanel>
            <Border
                x:Name="FormedFrameWindowTitle"
                Height="34"
                Background="#2D2D30"
                DockPanel.Dock="Top"
                MouseLeftButtonDown="Window_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel
                        Margin="10,0,0,0"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <Image
                            Width="18"
                            Height="18"
                            Margin="0,0,8,0"
                            Source="/Resources/Icons/estimate_cost_icon_213382.ico" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="14"
                            Foreground="White"
                            Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <Button
                            x:Name="MinimizeButton"
                            Width="40"
                            Height="30"
                            Click="MinimizeButton_Click"
                            FontSize="16"
                            ToolTip="Свернуть">
                            ─
                        </Button>
                        <Button
                            x:Name="MaxRestoreButton"
                            Width="40"
                            Height="30"
                            Margin="10,0,0,0"
                            Click="MaxRestoreButton_Click"
                            FontSize="16"
                            ToolTip="Развернуть">
                            ▢
                        </Button>
                        <Button
                            x:Name="CloseButton"
                            Width="40"
                            Height="30"
                            Margin="10,0,10,0"
                            Click="CloseButton_Click"
                            FontSize="16"
                            ToolTip="Закрыть">
                            ✕
                        </Button>
                    </StackPanel>
                </Grid>

            </Border>
            <Grid Background="{DynamicResource PrimaryColor}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  Левая панель  -->
                <Border
                    Grid.Column="0"
                    Margin="5"
                    Padding="10"
                    Background="{DynamicResource PrimaryColor}"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,0,1,0">

                    <ListBox
                        Margin="5"
                        Background="{DynamicResource PrimaryColor}"
                        BorderThickness="0"
                        Foreground="{DynamicResource PrimaryForeground}"
                        ItemsSource="{Binding SettingsItems}"
                        SelectedItem="{Binding SelectedSetting}">

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    Margin="0,4"
                                    Padding="12"
                                    CornerRadius="6">
                                    <TextBlock FontSize="15" Text="{Binding}" />
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                    </ListBox>
                </Border>

                <!--  Правая часть  -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border
                        Grid.Row="0"
                        Margin="20"
                        Padding="20"
                        Background="{DynamicResource PrimaryColor}"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="8">

                        <ContentControl Content="{Binding CurrentView}" />
                    </Border>

                    <StackPanel
                        Grid.Row="1"
                        Margin="0,0,20,20"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">

                        <Button
                            Width="110"
                            Height="36"
                            Margin="0,0,10,0"
                            Command="{Binding UpdateAllParametersCommand}"
                            Content="Сохранить" />

                        <Button
                            Width="110"
                            Height="36"
                            Click="CloseButton_Click"
                            Content="Отмена" />
                    </StackPanel>
                </Grid>
            </Grid>
        </DockPanel>
    </Border>
</Window>