<UserControl x:Class="ReportEngine.App.Views.Settings.CalculationParameters.Controls.HumanCostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid Margin="5">
            <Border
                Padding="5"
                Background="{DynamicResource PrimaryColor}"
                BorderBrush="{DynamicResource SecondaryColor}"
                BorderThickness="1"
                CornerRadius="8">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Заголовок  -->
                    <TextBlock
                        Grid.Row="0"
                        Grid.ColumnSpan="2"
                        Margin="0,0,0,10"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Text="Трудозатраты" />

                    <DataGrid
                        Grid.Row="1"
                        Grid.ColumnSpan="2"
                        BeginningEdit="DataGrid_BeginningEdit"
                        CanUserAddRows="True"
                        ItemsSource="{Binding HumanCost}"
                        LoadingRow="DataGrid_LoadingRow"
                        MouseDoubleClick="DataGrid_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="0.75*" Header="Наименование" Binding="{Binding Name}"/>
                            <DataGridTextColumn Width="Auto" Header="Значение" Binding="{Binding Value}"/>
                            <DataGridTextColumn Width="Auto" Header="Ед. изм" Binding="{Binding Unit}"/>
                            <DataGridTextColumn Width="0.75*" Header="Пояснение" Binding="{Binding Description}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>