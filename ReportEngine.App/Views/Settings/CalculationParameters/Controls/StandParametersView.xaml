<UserControl
    x:Class="ReportEngine.App.Views.Settings.CalculationParameters.Controls.StandParametersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid Margin="20">
            <Border
                Padding="25"
                Background="{DynamicResource PrimaryColor}"
                BorderBrush="{DynamicResource SecondaryColor}"
                BorderThickness="1"
                CornerRadius="8">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Заголовок  -->
                    <TextBlock
                        Grid.Row="0"
                        Grid.ColumnSpan="2"
                        Margin="0,0,0,10"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Text="Стенды" />

                    <DataGrid
                        Grid.Row="1"
                        Grid.ColumnSpan="2"
                        BeginningEdit="DataGrid_BeginningEdit"
                        CanUserAddRows="True"
                        ItemsSource="{Binding StandsParameters}"
                        LoadingRow="DataGrid_LoadingRow"
                        MouseDoubleClick="DataGrid_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="0.75*" Header="Должность" />
                            <DataGridTextColumn Width="*" Header="Имя" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>