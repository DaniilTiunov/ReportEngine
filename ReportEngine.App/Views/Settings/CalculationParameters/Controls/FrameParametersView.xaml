<UserControl
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d"
    x:Class="ReportEngine.App.Views.Settings.CalculationParameters.Controls.FrameParametersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid Margin="5">
            <Border
                Background="{DynamicResource PrimaryColor}"
                BorderBrush="{DynamicResource SecondaryColor}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="5">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Заголовок  -->
                    <TextBlock
                        FontSize="18"
                        FontWeight="SemiBold"
                        Grid.ColumnSpan="2"
                        Grid.Row="0"
                        Margin="0,0,0,10"
                        Text="Рамы" />

                    <DataGrid
                        BeginningEdit="DataGrid_BeginningEdit"
                        CanUserAddRows="True"
                        Grid.ColumnSpan="2"
                        Grid.Row="1"
                        ItemsSource="{Binding FrameCost}"
                        LoadingRow="DataGrid_LoadingRow"
                        MouseDoubleClick="DataGrid_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Binding="{Binding Name}"
                                Header="Наименование"
                                Width="0.75*" />
                            <DataGridTextColumn
                                Binding="{Binding Value}"
                                Header="Значение"
                                Width="Auto" />
                            <DataGridTextColumn
                                Binding="{Binding Unit}"
                                Header="Ед. изм"
                                Width="Auto" />
                            <DataGridTextColumn
                                Binding="{Binding Description}"
                                Header="Комментарий"
                                Width="0.75*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>