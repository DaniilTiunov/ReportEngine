<Window
    x:Class="ReportEngine.App.Views.Windows.UsersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReportEngine.App.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ReportEngine.App.ViewModels"
    Title="Управление пользователями"
    Width="1000"
    Height="700"
    Closing="Window_Closing"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Height" Value="40" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Background" Value="#FFDDDDDD" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="10" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="#FFF5F5F5" />
            <Setter Property="AlternatingRowBackground" Value="#FFE0E0E0" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="89*" />
            <ColumnDefinition Width="63*" />
            <ColumnDefinition Width="43*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid
            x:Name="UsersDataGrid"
            Grid.Row="1"
            Grid.ColumnSpan="4"
            Margin="10,79,5,10"
            CanUserAddRows="True"
            CellEditEnding="UserDataGrid_CallEditEnding"
            ItemsSource="{Binding CurrentUser.AllUsers, Mode=OneWay}"
            RowHeaderWidth="40"
            SelectedItem="{Binding CurrentUser.SelectedUser}"
            SelectionUnit="FullRow">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding ShowAllUsersCommand}" Header="Показать всех" />
                    <MenuItem Command="{Binding DeleteUserCommand}" Header="Удалить выбранного пользователя" />
                    <MenuItem Command="{Binding AddNewUserCommand}" Header="Добавить" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding SecondName, UpdateSourceTrigger=PropertyChanged}"
                    Header="Фамилия" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                    Header="Имя" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                    Header="Отчество" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Position, UpdateSourceTrigger=PropertyChanged}"
                    Header="Должность" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Cabinet, UpdateSourceTrigger=PropertyChanged}"
                    Header="Кабинет" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                    Header="Почта" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding PhoneContact, UpdateSourceTrigger=PropertyChanged}"
                    Header="Номер для связи" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
