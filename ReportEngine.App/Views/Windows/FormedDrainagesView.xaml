<Window
    x:Class="ReportEngine.App.Views.Windows.FormedDrainagesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Формированные дренажи"
    WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!--  Левая часть: Список дренажей и параметры  -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Список дренажей  -->
            <GroupBox
                Grid.Row="0"
                Margin="10"
                Header="Список сформированных дренажей">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <DataGrid
                        Grid.Column="0"
                        Margin="5"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        ItemsSource="{Binding FormedDrainagesModel.AllFormedDrainage}"
                        SelectedItem="{Binding FormedDrainagesModel.SelectedFormedDrainage}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Name}" Header="Наименование" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <GroupBox
                        Grid.Column="1"
                        Margin="5"
                        Header="Назначения дренажа">
                        <StackPanel>
                            <DataGrid
                                Margin="5"
                                AutoGenerateColumns="False"
                                CanUserAddRows="False"
                                ItemsSource="{Binding FormedDrainagesModel.Purposes}"
                                SelectedItem="{Binding FormedDrainagesModel.SelectedPurpose}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding Purpose}" Header="Назначение" />
                                    <DataGridTextColumn Binding="{Binding Material}" Header="Материал" />
                                    <DataGridTextColumn Binding="{Binding Quantity}" Header="Кол-во" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <Button
                                    Margin="5"
                                    Command="{Binding AddPurposeCommand}"
                                    Content="Добавить назначение" />
                                <Button
                                    Margin="5"
                                    Command="{Binding DeletePurposeCommand}"
                                    Content="Удалить назначение" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </GroupBox>

            <!--  Добавление/редактирование дренажа  -->
            <GroupBox
                Grid.Row="1"
                Margin="10"
                Header="Данные дренажа">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="5"
                        VerticalAlignment="Center">
                        Наименование:
                    </TextBlock>
                    <TextBox
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="5"
                        Text="{Binding FormedDrainagesModel.SelectedFormedDrainage.Name, Mode=TwoWay}" />
                </Grid>
            </GroupBox>

            <!--  Кнопки управления  -->
            <StackPanel
                Grid.Row="2"
                Margin="10"
                HorizontalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    Margin="5"
                    Command="{Binding AddFormedDrainageCommand}"
                    Content="Добавить дренаж" />
                <Button
                    Margin="5"
                    Command="{Binding DeleteFormedDrainageCommand}"
                    Content="Удалить дренаж" />
            </StackPanel>
        </Grid>

        <!--  Правая часть: Справочник дренажей  -->
        <Grid Grid.Column="1">
            <TabControl Margin="10">
                <TabItem Header="Комплектующие">
                    <DataGrid
                        Margin="5"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        ItemsSource="{Binding FormedDrainagesModel.DrainageDetails}"
                        SelectedItem="{Binding FormedDrainagesModel.SelectedFormedDrainage}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Name}" Header="Наименование" />
                            <DataGridTextColumn Binding="{Binding Cost}" Header="Цена" />
                            <DataGridTextColumn Binding="{Binding Measure}" Header="Ед. изм." />
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>