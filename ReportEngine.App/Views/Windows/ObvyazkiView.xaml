<Window
    x:Class="ReportEngine.App.Views.Windows.ObvyazkiView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:ReportEngine.App.Display"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Обвязки"
    Width="1400"
    Height="700"
    mc:Ignorable="d">
    <Window.Resources>
        <converters:ObvyazkaImagePathConverter x:Key="ObvyazkaImagePathConverter" />
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" /> <!-- Область для списка картинок -->
            <ColumnDefinition Width="Auto" /> <!-- Разделитель -->
            <ColumnDefinition Width="1.5*" /> <!-- Область для деталей выбранной картинки -->
        </Grid.ColumnDefinitions>

        <!-- Список больших картинок (слева) -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl
                Margin="10"
                ItemsSource="{Binding CurrentObvyazka.Obvyazki}"
                >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Margin="10"
                            Background="WhiteSmoke"
                            BorderBrush="LightGray"
                            BorderThickness="1"
                            CornerRadius="5">
                            <Image
                                Width="250"
                                Height="250"
                                Source="{Binding ImageName, Converter={StaticResource ObvyazkaImagePathConverter}}"
                                Stretch="Uniform"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Разделитель -->
        <GridSplitter
            Grid.Column="1"
            Width="5"
            Background="LightGray"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" />

        <!-- Детали выбранной обвязки (справа) -->
        <Border Grid.Column="2" Margin="10" Background="WhiteSmoke">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Уменьшенная картинка -->
                <Image Grid.Row="0"
                       Margin="10"
                       Width="200"
                       Height="200"
                       Source="{Binding CurrentObvyazka.SelectedObvyazka.ImageName, Converter={StaticResource ObvyazkaImagePathConverter}}"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"/>
                
                <!-- Детали обвязки -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <StackPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="14" />
                                <Setter Property="Margin" Value="0,5" />
                            </Style>
                        </StackPanel.Resources>

                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.Number, StringFormat='Номер: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.LineLength, StringFormat='Длина линии: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.ZraCount, StringFormat='Кол-во ЗРА: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.TreeSocket, StringFormat='Тройники: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.Sensor, StringFormat='Датчики: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.SensorType, StringFormat='Тип датчиков: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.Clamp, StringFormat='Хомуты: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.WidthOnFrame, StringFormat='Длина на раме: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.OtherLineCount, StringFormat='Др. линии: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.Weight, StringFormat='Масса: {0}'}" />
                        <TextBlock Text="{Binding CurrentObvyazka.SelectedObvyazka.HumanCost, StringFormat='Трудозатраты: {0}'}" />
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Кнопки действий -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                    <Button Content="Сохранить" Command="{Binding SaveUserCommand}" Margin="5" Padding="10,5"/>
                    <Button Content="Удалить" Command="{Binding DeleteUserCommand}" Margin="5" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>